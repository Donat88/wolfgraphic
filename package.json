{
  "name": "wolfgraphic",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "live-server --port=3000 --open=./index.html",
    "build": "echo \"Nincs build lépés, csak statikus fájlok\""
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
